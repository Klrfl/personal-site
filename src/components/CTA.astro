---
const { href } = Astro.props;
---

<a href={href} class="cta">
  <slot />
</a>

<style>
  .cta {
    outline: 2px solid var(--accent);
    margin: 1rem auto;
    padding: 1em 2em;
    display: inline-block;
    color: inherit;
  }
</style>
