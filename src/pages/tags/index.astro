---
import MyLayout from "../../layouts/MyLayout.astro";
const allPosts = await Astro.glob("../posts/*.md");

const tags = [...new Set(allPosts.map((post) => post.frontmatter.tags).flat())];
---

<MyLayout title="Tag Index">
  <ul>
    {
      tags.map((tag) => (
        <li>
          <a href={`/tags/${tag}`}>{tag}</a>
        </li>
      ))
    }
  </ul>
  <p>Anjing</p>
</MyLayout>
